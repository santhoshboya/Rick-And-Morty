query Episodes($page: Int) {
  episodes(page: $page) {
    info {
      count
      next
      pages
      prev
    }
    results {
      name
      id
      episode
      created
      air_date
    }
  }
}