export const ROUTE_EPISODES = '/episodes';
export const ROUTE_EPISODE_DETAILS = (id: string) => `/episodes/${id}`;
export const ROUTE_EPISODE_DETAILS_TAB = (id: string, tabId: string) => `/episodes/${id}/${tabId}`;
export const ROUTE_CHARACTER_DETAILS = (id: string) => `/character/${id}`;
